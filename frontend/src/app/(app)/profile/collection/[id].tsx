import { useLocalSearchParams } from "expo-router";
import { View } from "react-native";


const ProfileCollectionByIdPage = () => {
    const { id } = useLocalSearchParams();

    return (
        <View>

        </View>
    );
};

export default ProfileCollectionByIdPage;
