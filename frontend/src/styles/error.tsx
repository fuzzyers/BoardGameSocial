import { StyleSheet } from "react-native";

export const errorStyle = StyleSheet.create({
    error: {
        color: "#c62828",
        textAlign: "center",
        marginBottom: 16,
        fontSize: 14,
    },

    inputError: {
        borderWidth: 1,
    },
});